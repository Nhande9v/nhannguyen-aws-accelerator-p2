apiVersion: v1
kind: Service
metadata:
  name: demo-app-stable
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: demo-app
---
apiVersion: v1
kind: Service
metadata:
  name: demo-app-canary
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: demo-app